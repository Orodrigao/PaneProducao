.next/
node_modules/
.env.local
.env
out/
tsconfig.tsbuildinfo
.vercel
supabase/.temp/
_GUIA_INFRA.md

# Anexos das sessões de IA (prints e fotos coladas no chat).
# Podem conter segredo, dado real de cliente ou documento fiscal:
# nunca entram num commit.
.codex-remote-attachments/

# Configuração local do Claude Code (por máquina, não versionar)
.claude/*
# Exceção: skills compartilhadas viajam no repositório (ver AGENTS.md)
!.claude/skills/

# Sessão autenticada, downloads e evidências locais do coletor CNM
storage/cnm/
playwright/.auth/
.storage-state/
storage-state*.json
test-results/
playwright-report/
blob-report/
